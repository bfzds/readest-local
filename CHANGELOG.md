# 更新日志（Changelog）

本文件基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0) 格式维护。本仓库为个人 fork，tag 采用 `cef-preview-*` 形式的日期快照，**不遵循语义化版本（SemVer）**。

## [Unreleased]

### Added

- **PDF 新增「锁定水平平移」开关（移植上游 #6030）**。固定版式（PDF/CBZ）放大后水平拖动会漂移，开启后翻页保持当前横向偏移、只允许纵向移动；横向滚动模式（读的就是 x 轴）下该开关禁用。视图菜单 PDF 区块可切换，随书/全局作用域按既有机制持久化（依赖 foliate-js 上游 #89 补丁，已随子模块一并同步）。
- **词典词条图片可点开放大、词性锚点可跳转（移植上游 #6021）**。MDict 词条里的插图点击后全屏查看（同组取最大的全分辨率副本，缩略图放大没有意义）；`entry://#anchor` 与裸 `#anchor` 这类词条内链接（牛津系词典的词性切换器）不再被当成词头去查询，而是在已渲染的词条里滚到目标节（兼容老式 `<a name>` 目标）。另外 MDict 的音频/图片 blob 现在按扩展名带上 MIME 类型，无类型 blob 在 WebKit 上会解码失败。
- **朗读新增「跳过括号内注音」开关（移植上游 #6057）**。打开后，正文中「汉字（假名/汉字）」形式的括号注音不再被朗读出来——HTML ruby 注音此前已处理，这条补齐纯文本括号注音，句子枚举与 SSML 两条朗读路径都过滤。设置 → 朗读里新增开关（简繁界面都有；上游把开关限定在日语界面显示，本分支界面只有中英，限日语会令开关不可达，故改为所有界面语言可见，上游对应测试同步改写）。已知边界：括号内是拉丁字母拼音（如「汉字（hàn zì）」）不在上游刻意保守的匹配范围内（只认假名与汉字字符，避免误伤正文），不会被跳过。
- **监控文件夹独立化 + 多文件夹监控 + 按作者分组**。三件事一起落地。① **监控不再要求「原位置取书」**：此前自动导入的复选框只在"原位置取书"打开时才出现（对话框层由 `effectiveReadInPlace && autoImport` 派生，落库层勾了自动导入必然连带登记外部书库），根因在导入层——只有原位置导入才把源路径写进 `book.filePath`，复制模式的书每次重扫都要完整解析 + 重算 partialMD5，上千本书的目录每次回前台全量重哈希基本不可用。现在监控与取书方式解耦：未开原位置时书籍**复制**进书库（复选框下方直接写明，并说明从书库删书不影响源文件），同时把源路径记进 `book.altFilePaths`，重扫改为按路径比对、与原位置模式同价；为救回本次改动之前就用复制模式导入的老书，hash 命中的提前 return 短路里也补记一次（首次重扫一次性补齐，之后走账本）。**账本变化本身会触发落盘**——重扫命中的都是"已在书库"、不计入成功导入，只补记路径的那次若不写盘，账本随进程消失、下次重扫又整目录重新解析。② **多文件夹**：新增「监控文件夹」管理对话框（书库导入菜单 + 设置 → Custom 两处入口），逐行可选分组模式、文件格式与最小体积，单行「立即刷新」与顶部「立即刷新全部」，并显示每个文件夹上次扫描的时间与结果（"新增 N 本 / 无新增 / 失败 N 个"，只存组件状态与 localStorage，不落设置）；扫描参数按文件夹持久化在新增的 `autoImportFolderRules`（设备本地，与两个旧数组一起进 `BACKUP_SETTINGS_BLACKLIST`），旧的 `autoImportFlattenFolders` 只保留读取兼容、不再新增写入，因此**已监控的老文件夹行为完全不变**。③ **新增第三种分组模式「按作者分组」**：面向 `<下载目录>/<站点>/<下载日期>/<作者>/<小说本体>` 这类结构，组名为 `<监控文件夹>/<作者>`，忽略下载日期段（识别 `YYYY-MM-DD`、`YYYY.MM.DD`、`YYYY_MM_DD`、`YYYYMMDD`、`YYYY年M月D日`；纯数字用户 ID 不判为日期，为此 8 位紧凑日期额外要求月份 01–12、日期 01–31），更深的层级丢弃（`<作者>/<系列>` 这类分组本次不做）。顺带把"手动导入对话框里选的格式与最小体积"也写进该文件夹的规则，避免"对话框里选了格式、之后重扫又按默认格式漏掉小文件"的不一致。
- **EPUB 新版导入：自动认出书库里的旧版本，确认后覆盖并完整保留阅读进度**。此前导入一本换源/改版重下的 EPUB 只有两条路：文件字节完全一致才去重；或者元数据（标题+作者+标识符）算出同一个 metaHash 时才静默合并——而换一个下载源，`dc:identifier` 里的 UUID 必然不同，于是同一本书在书架上并排出现两本，老进度留在旧的那本上。现在手动导入（拖放/文件选择器/目录导入）的 EPUB 会额外做一次宽松匹配：归一化标题（NFC、剥掉成对的《》【】「」等包裹符、去空白、小写）+ 首个作者 + 同格式，命中书库里的另一本时按"新书"正常落盘，并在整批导入结束后弹一次确认框，逐条选择「用新版替换（保留进度）」「保留为两本」或「撤销导入」；关掉窗口不做选择 = 两本都留着，不会有任何数据被删。选择替换时把旧书的 `config.json`（阅读位置、页码进度、书签笔记、阅读设置、虚拟目录）整体迁到新 hash 目录，并把只存在于书目记录上的用户数据（进度条、分组、标签、阅读状态、手动排序、**用户改过的书名**）搬到新记录，旧记录从 library.json 移除、旧目录清理、阅读时长统计库按新 hash 改名；书签的 `bookHash` 一并改写，不再挂在旧 hash 上。**同时修复了一个踏进这个功能就会立刻暴露的坑**：保存的阅读位置是 EPUB CFI，指向的是旧文件的内部结构，新版本一旦重排章节，这个 CFI 要么解析不到、要么落到错误位置——现打开书时先探测 CFI 能否在新文件里定位（`resolveCFI` 返回 -1 即失效），失效或抛错时按 `progress` 的百分比落位，而不是跳回第一页或白屏；深链 `?cfi=`（批注导出链接）不走这条兜底，解析不到就落在正文开头，因为它要的是"用户点的那一处"，悄悄变成"我上次读到哪"会读成"链接被忽略了"。匹配范围后来扩大了（见 Changed 一节）：同名 PDF 与改版 TXT 也纳入，判据改成"书号一致（PDF 的书号是文件名字盐）+ 同名同作者"，且导入路径不再自动合并任何东西。
- **主窗口死信看门狗（#2）**：webview 白屏或加载失败时自绘标题栏随页面一起失效，窗口此前没有任何可见的关闭途径，只能任务管理器杀进程。现主窗口 JS 每 5 秒发 `main-window-alive` 心跳，Rust 侧看门狗 20 秒收不到即 `show()` 窗口并恢复原生装饰——OS 级标题栏（含关闭按钮）直接出现在故障页面上；心跳恢复后自动还原自绘界面。dev 模式下触发时额外弹原生提示框，说明 next dev 未就绪/挂起及 `.next` 缓存修复方法。macOS 本就有原生装饰，不参与。同时顺带修复了「前端未加载时窗口永不显示」的隐藏问题。
- **EPUB 虚拟目录**：目录元数据退化（如只有「信息/目录」等文件级条目）但正文有章节标题行的 EPUB，可在侧栏通过「正则预选 + 命中预览」引导弹窗生成虚拟目录；支持内置规则与自定义正则、按文件分章兜底；目录按书持久化（存 config.json，不改 EPUB 文件），侧栏显示、点击跳转、当前章节书本图标高亮齐备，重开书不丢失。同时抽出 TXT/EPUB 共享的章节正则引擎（含 ReDoS 守门与缓存）。

### Changed

- **备份导出改为只包含书库中存活的书目（移植上游 #5851）**。此前按 `Books/` 目录全量打包，已删书残留的文件、导入中断留下的目录都会被静默打进 zip；现在按 library.json 的存活行过滤（空库/加载失败时仍全量导出，以便恢复重建）。缓存管理对话框同步支持「孤儿书籍文件」识别与清理，会显示孤儿数量并在确认文案里写明。导出范围连带改为只导书目目录（`Books/<hash>/`）：应用自建的根级文件（library.json 家族、library.db 家族）本来就在旧排除名单内，运行期文件（锁与锁日志）位于应用数据目录、不在扫描范围内，因此这处收窄对现状没有实际影响。

- **Ctrl+滚轮调字号不再受「默认字号」封顶（吸收上游 v0.12.8 行为优势，未采纳其改写默认设置的路线）**。实时字号最多可放大到默认值的 1.5 倍（绝对上限 120px），「默认字号」设置本身仍绝不被滚轮改写、只作锚点，下限仍是最小字号；书内图片随字号同步放大。实时字号照旧持久保留、不自动重置，向下滚或改设置即归位，浮层始终显示真实 px。
- **固定版式书（PDF/漫画等）Ctrl+滚轮改为缩放页面（50%–500%，重启保留），不再走对固定版式无意义的字号调节**；触屏捏合路径不变，且顺带补上捏合缩放此前缺失的落盘（此前重启会丢缩放比例）。
- **目录中超长章节标题改为换行显示，不再单行截断（移植上游 #5858）**。标题容器改为可收缩换行（`min-w-0 break-words`），页码加 `shrink-0` 不被挤走；目录列表本就逐行测量高度，无需其他适配。
- **目录导入对话框：自动导入不再是「原位置取书」的子选项，结构模式多出「按作者分组」**。原来的 Watched Folders 子页（只在已有监控文件夹时出现、只能改"结构/停看"两项）改为一行入口，打开上面那个管理对话框——两处列表 UI 合并成一处。其余字段与文案不变。
- **版本冲突弹窗的字号与尺寸调整**。实测反馈弹窗偏小：对比表、"判定依据"、差异摘要这些要逐字核对的数据一律 12px，逐条按钮文案在 600px 宽的弹窗里被折成两行。现把数据文字抬到 13px、每条书名抬到 15px（细字说明仍留 12px 以保留层次），表格行距与数值对齐一并调整；弹窗在桌面横屏下的可用宽度从约 480px 提到约 656–696px（`Dialog` 的默认值未动，只在本弹窗用 `!` 前缀覆盖宽度与内容边距，避免波及设置等其它弹窗），按钮文案因此不再换行。
- **导入不再自动合并任何书**。此前按"书号相同"命中时会原地接管旧记录：把旧 `config.json` 搬到新目录、再从 library.json 移除旧记录并删掉它的整个 `Books/<hash>/`（书文件、阅读位置、书签、封面、nav 缓存）——用户从未被问过，而且"书号"这件事对 PDF 只是文件名、对换源重下根本对不上，判据本身也不可靠。现在这条路径整体删除，凡是"导入时认定书库已有同一本"一律改成弹窗确认，并在弹窗里并列两边的书名/作者/书号/大小/修改时间/**正文字数**/**章节数**（目录不完整的书改用正文规模对比并注明原因）：正文字数与自带目录由原生解析器或 TXT 转换器在导入时顺带算出，弹窗打开时不解析任何文件。逐条可选「用新版替换（保留进度）」「保留为两本」或**「撤销导入」**——最后一项是这次新增的动作：书库里的是新版、导入的才是旧版时，一键丢开刚导入的那本、书库保持原样（纪录片留作墓碑，受监视文件夹的重扫才不会反复把它当新文件弹窗）。**同名 PDF 现在也走这套弹窗**：PDF 的书号是文件名字盐（#5411），同名即同书号；不同名的 PDF 仍不会互相认作版本，那正是 #5411 要防的样板元数据误判。判定收拢在唯一一处产出（`findIncomingVersionConflict`），导入路径与"批后二次探测"共用——一次拖入多个版本时批内两本彼此看不见（对方还没入库），整批落盘后补一次互查，每对只报一次。受监视文件夹重扫、双击/「打开方式」这类静默路径不弹模态框（前者是窗口重新获得焦点时的后台动作，后者发生在应用启动、紧接着就导航去阅读器）：重扫给一条可点击通知，双击那条等你从阅读器回到书库时再问——待问冲突存在与页面无关的队列里，且弹窗只取只读快照、等用户答完才落定，所以书库页被导航卸载不会把它们带走。**任何会在书库留下记录的路径都不再静默删书，最多"晚点问"**（`autoImportBooksOnOpen` 关闭时双击打开是临时预览、不入库，也就不存在冲突）。
- **同一个文件重复导入不再刷新字段、也不再打扰**。命中既有记录时不再顺手抬 `createdAt`/`updatedAt`、重写元数据——"把同一本书再拖一次"曾经会悄悄改掉书库排序与同步时钟。现在记录还在时只提示「已在书库中：<书名>」，一个字段都不动（也不复制文件、不重写 config）；记录被删过则复活它并提示「已从书库恢复」，否则删掉的书拖回来永远拿不回来。TXT 先查重的短路面（`sourceHash` 命中）语义同步对齐。

### Removed

- **导入路径上的自动折叠与重复清扫（`mergeBooks`）**。这条路径会连带删除既有记录及其整个 `Books/<hash>/` 目录，改由用户确认后经 `replaceBookVersion` 执行；历史遗留的同键重复不再被自动清理，改为在下一次导入时按弹窗询问。同一份文件的两个副本（字节相同、hash 相同）也不再并成一条——两条记录各自记住自己的来源路径，重扫依旧不会反复导入。

### 已知限制

- **监控文件夹的源路径账本只记路径，不记大小/时间戳**：源文件被替换（同名同路径、内容不同）不会被重新导入，与原位置模式的行为一致；要区分得给扫描结果带上 size/mtime，本次没做。
- **「按作者分组」丢弃作者层以下的层级**（`<作者>/<系列>` 这类分组本次不做），且只识别下载日期段（含 Windows 的 ` (2)` 去重后缀，不含 `_backup` 这类自由后缀）；若某个作者目录恰好取名成 `20250101` 这种"看起来完全像日期"的纯数字（用户 ID），它会被当成日期层跳过。识别规则本身要求年份以 19/20 开头、月日合法，普通的 7–10 位纯数字 ID 不受影响。
- **同标题、同标识符但实为两篇的情况无法靠元数据识别**，导入时会被当作同一本的两版并列问一次。代价是"多一条书目"，不再是"少一本书"：导入路径已经不删任何记录，判断错了也只是书架上多一本，用户选「保留为两本」即可。
- **一次拖入的多个文件是并发导入的**，同一批里的"新旧版本"彼此看不见（对方还没入库）。整批落盘后会做一次"批后二次探测"补上互查，因此批次内部的版本对不会再漏判；这条限制现在只在"三条以上版本混在同一批、且用户想逐对挑选"时表现为提示分组不够细。

### Fixed

- **便携版构建恢复：Cargo workspace 缺 `homepage`/`repository` 让 `cargo metadata` 解析失败**。2026-09-19「与上游断开」把这两项从根 `Cargo.toml` 的 `package` 表删除，但 vendored 的 tauri 子模块各 crate 仍以 `homepage.workspace = true` / `repository.workspace = true` 继承，键不存在会让构建连清单解析都过不去（现象：`pnpm --filter readest-local tauri build` 报 `workspace.package.homepage was not defined`）。TS 侧单测与 lint 不跑 cargo，故此问题在代码批次里不会被发现。现按 `apps/readest-app/src-tauri/Cargo.toml` 的既有做法在根表把两项定义为空串——不冒充上游身份，同时满足 workspace 继承。
- **上游 0.12.x「B 档」健壮性与体验修复移植（10 个上游提交 + foliate-js 5 个补丁）**。① 大批量导入与监控文件夹扫描增加 15 秒节流的周期性落盘，中途退出/崩溃不再丢掉整批索引（#5615）；② 书库数据目录被删或所在磁盘拔掉时，启动不再白屏——探测不可用目录并在提示里点名它、指路到设置重选，同时修正「init 失败后缓存半成品服务」的单例时序问题（#5789）；③ TTS 段间停顿不再随语速被重复缩短（#5753，2x 语速下段停顿此前被压到约 0.085s 趋近于零；现在缩放只发生在一处。注：本分支的句间停顿设置 `setSentenceGap` 已是空实现、无消费点，句间停顿本身不受影响）；④ PDF 跨行复制粘贴出来是整段而不是一行一个断行（#5828，按文本层几何重建段落）；⑤ 含 `epub:switch` 的 EPUB 3 按浏览器能渲染的分支取内容，不再被清洗器连回退内容一起删掉（#480）；脚本化 canvas 在脚本运行期间也能正确排版与限宽（foliate-js #90），DITA 风格的内联公式不再被误包块级滚动容器；⑥ PDF 页面未变化时不再冗余重渲染，朗读 PDF 时翻页/缩放不再中断朗读（foliate-js #91）；⑦ CBZ 分卷按页路径逐段比较排序，卷 10 不再排在卷 2 前面（foliate-js #79）；⑧ 分页跨章节边界时搜索结果高亮保持可见（#5725，foliate 的位置范围只覆盖主 section）；⑨ 取消勾选「原位置取书」即反注册该文件夹，之后的导入重新复制入库而不是继续就地读取（#5685，已注册文件夹此前被强制锁定）；
- **上游 0.12.x「A 档」排版与朗读修复移植（15 个上游提交）**。① 默认字体设置不再覆盖书内嵌字体（#6047）：`html` 选择器改零特异性的 `:where(html)`，pre/code/kbd 规则按"覆盖字体"开关换边，带空格的字体名不再被通用字体替换逻辑改坏——此前设了默认字体后，书内 CSS 指定的嵌入字体会被顶掉；② 部分书内 CSS 类选择器、属性选择器全部失配（书"没有样式"的常见根因）——XHTML 命名空间未声明——已恢复，章节片段与 `[xml|lang]`/`:lang()` 语言匹配一并修复（#6040/#6054/#6090），顺带把 `body { line-height: unset }` 从页面排版层移回段落排版层，「使用书籍排版」下书籍自身的 body 行高不再被顶掉；③ 固定版式书：禁用 WebView 文字自动放大（#5659，安卓上逐字定位文本错位的根因），深色模式按书内原样渲染、不再整页反色加底色（#5657，PDF/漫画这类应用渲染的页面保持主题配色）；④ 排版修复：两端对齐不再出现过大词距（#5718）、宽表格的标签列不再塌缩成一列窄条（#5686）、`position:fixed` 背景不再钉住视口且 Duokan 负水平边距归零不再横向溢出（#5729）；⑤ 波斯/阿拉伯文本中被误用作半空格的 RLM 标记转回 ZWNJ 正常显示（#5651）；⑥ Markdown 支持 tab 缩进的脚注定义（#5975）、导入书名优先取文件名而非首行大标题（#5653）；⑦ 空书库迁移数据目录后新位置能被正确保存（#5878，"扫描失败"与"空库"已区分，前者不再误放行迁移——避免迁移后删掉旧目录却什么都没拷走）；⑧ Ctrl/Cmd+R「朗读选中内容」改为只读选中的文字、读完即停（#6076，此前漏传一次性标志，会从所在段落开头一直读下去变成普通朗读会话；朗读按钮同理，不再把点击事件对象误当标志传入）。
- **老监控目录"打开一次导入对话框按 OK"就被悄悄收窄扫描范围**。挂载时的表单种子取自 localStorage 里的"上次导入"取值，而不是当前目录自己的规则——对"在旧版本监控过、规则表里没有条目"的目录（即所有升级用户），打开对话框直接确认会把它的扫描格式改成上次导入选的格式（例如从"全部支持格式"收窄成 EPUB+PDF），扁平导入的目录还会被改成按子文件夹建组，全程无提示。现在挂载与换目录共用同一套解析（该目录自己的规则优先，只有不被监控的目录才用上次导入的取值）；另外，未改动的格式/体积不再回写规则——老目录解析出的完整支持列表里有对话框无法表达的扩展名（`md` 没有对应的格式组），回写等于把它删掉；确实需要回写时（该目录已有规则，或用户改了选择）也会把这些组外扩展名一起带上，不会因为"对话框只能整组勾选"而丢失。
- **备份恢复里"复活已删的书"会被防复活护栏吞掉**。备份恢复把"备份里存活、本地已删"的书复活时会清掉墓碑，但没盖复活标记，于是书库保存的护栏把它当成"陈旧窗口想复活已删的书"整行丢弃：界面显示已恢复、重启后又不见。现恢复路径与导入路径一样盖复活标记（`revivedAt`）。
- **「从文件夹导入」的监控开关跟着"上次导入的文件夹"走，一次普通确认就静默停看并删掉规则**。对话框里的"监控此文件夹"只在打开时播一次种，取自 localStorage 里的**上次导入目录**而非用户刚选的那个：在 A 已监控、上次导入的是未监控的 B 时打开对话框，勾选框显示未勾；把目录改成 A 直接确认，A 被移出监控列表，连它的结构/格式/体积规则一起删掉，全程无提示（加规则表之前这已是"未勾即停看"，加规则表之后升级成"未勾即丢配置"）。URL 入口/拖拽目录重放那条非对话框通路同样会停看，并以合成的"保持结构"压过该文件夹已存的规则。现在对话框每次换目录都按**该目录自己的**状态重播表单（监控开关、结构模式、格式、最低体积；挂载时也先按该目录的存量规则取值），确认写回的一定是描述这个文件夹的值；非对话框通路完全不碰监控状态与规则，分组按该文件夹自己的规则解析。
- **管理页里勾选/取消一个格式组只改了组内第一个扩展名**。MOBI/AZW/AZW3、CBZ/ZIP 这类一个勾选框对应多个扩展名的分组，取消勾选时只移除 `mobi`（或 `cbz`），`azw`/`azw3`（或 `zip`）仍在扫描规则里被导入，界面却显示为未勾。现按整组切换。
- **被导入带回来的书会被"防复活"护栏整行丢弃：界面上回来了、重启后又消失**。书库保存的合并逻辑有一条护栏——磁盘上是墓碑（已删除）、传入记录没有墓碑时，视为"陈旧窗口想复活已删的书"而丢弃该行；而导入路径把删过的书带回书架时正是"清掉墓碑、无墓碑"的形状，于是那次保存被丢掉（一并丢掉这次补记的来源路径，导致那本书每次重扫都要重新解析）。现导入路径清墓碑时在记录上盖一个复活时刻（`revivedAt`），护栏只对这一种情况放行；陈旧窗口的副本没有该标记，仍被拦下（不能用 `updatedAt` 判：陈旧窗口只要在别处删书后翻过一页，它的 `updatedAt` 同样会更新）。刷新反馈一并更正：复活算"新增"而非"无新增"。
- **「按作者分组」忽略带 Windows 去重后缀的日期目录**。`2025-01-01 (2)`（重新下载时系统给同名目录加的后缀）此前不被认作日期层，被当成作者，组名产出 `站点/2025-01-01 (2)`。现识别 ` (n)` 形式后缀；`2025-01-01_backup` 这类自由后缀仍不识别——无法与"以日期开头的作者名"区分，猜错会整层作者被吞掉。
- **「按作者分组」在只导入、不监控时被静默忽略**。规则是按文件夹存进设置的，而"导入这个文件夹但不勾监控"不会写任何设置，于是分组那一步回退到默认的镜像模式——对话框明明写着「按作者分组」，书却按 `<文件夹>/<日期>/<作者>` 完整镜像落了组。现由导入批次显式携带本次对话框选的那套规则，优先于设置里的存量（新增 `resolveImportBatchFolderRule`，把这条优先级固定下来并加了回归用例）。
- **手动刷新与进行中的导入批次重叠时误报「没有发现新书」**。`importBooks` 遇到正在跑的批次会直接返回空结果（防并发），而手动刷新把"空结果"当成"这轮没有新书"展示，并把各文件夹的扫描状态覆盖成"无新增"；新增 `importingRef` 与 `loading` 状态双闸门（同一次点击里状态还没重新渲染，只读 state 会读到过期的 `false`），并在目录遍历结束、真正发起导入前再查一次——两个方向都改为明确提示"还有一批导入正在进行"。
- **「撤销导入」之后再导入同一个文件会静默复活、且从此不再询问**。撤销导入写的是墓碑记录（为了受监视文件夹重扫能认出这个文件、不把它反复当新文件扫出来），而导入路径遇到墓碑的动作是"复活并早退"，版本冲突探针又只在"库里没有这本书的记录"时才跑——于是用户刚在弹窗里否掉的那本又默默回到了书架，而且这个文件以后再也不会被问一次。现给墓碑加一个"本次导入已被用户拒绝"的标记（`importRejectedAt`）：带这个标记的记录重导时按新记录处理，走完整导入路径——重新解析、按需把书文件补回（撤销已经把 `Books/<hash>/` 清掉了，此前复活得到的是一本"活着但打不开"的书），并重新弹窗确认；用户重新接受后标记清除，同一个文件再导一次就回到「已在书库中」的不打扰路径。书库里的普通删除不写这个标记，重导行为不变（静默复活 + 「已从书库恢复」）。
- **跨窗口书库锁的"毒锁"：一次保存写完却没释放，之后全应用所有保存只能等超时失败**。真机实测：一次"删除书籍后保存"拿到锁（锁文件 token 反解出的获取时刻 15:56:38.883），写完 `library.json.bak`（.908）与 `library.json`（.916）之后**锁没有被释放**，而 Rust 后台的心跳线程与持有者的生命周期完全解耦，继续每 3 秒把锁文件 mtime 续成"新鲜"（实测恒为 3.000 秒间隔）——两条陈旧判据（"mtime 早于进程启动"、"年龄超过 10 秒"）因此永远不成立，锁再也不会被回收。表现：此后每一次保存都要等 5 秒捕获超时再失败（导入、替换、删除、连阅读进度保存全中），书库从那一刻起不再落盘，界面反复弹「保存书库失败，本次更改可能未保存」，而书只存在内存里、重启即丢。改动三处：① **存活判定改为租约**——删掉 Rust 后台心跳，改由持锁的 JS 每 3 秒调新增的 `renew_library_lock` 续期，持有者一结束（含释放失败、WebView 销毁）就再没人保鲜，锁在陈旧阈值（10s→30s）后自动可回收；② **释放自愈**——`release_library_lock` 改为带退避重试（Windows 下杀毒/索引器短时占用会让 remove 报 sharing violation），并新增 `library.lock.log`：获取、续期、自动回收、释放失败各写一行（此前这类故障只出现在 WebView 控制台里，定位只能靠反解 token 时间与文件 mtime）；③ **临界区顺序**——改为"先进本窗口串行链、再取跨窗口锁"（原先反过来：排队中的保存会握着锁等前面的保存跑完，把临界区拉长、也放大了"写完了却没能释放"的窗口）。残余：持锁窗口若异常死亡，其它窗口最多有 30 秒的保存会先失败（旧实现是永久失败），日志里能查到对应记录。
- **TXT 目录识别被下载器头部带偏，中文书整本退化成序号分章**。章节规则的语言此前交给 `franc` 判正文前 1000 字符，而小说下载器导出的 TXT 开头是「题名/作者/Tag列表/原始网址/封面图片地址/简介/下载时间」加若干长 URL，这段文本里 ASCII 常多于汉字（实测样本 724 : 191），franc 给出 `eng`；`buildChapterRegexps` 于是一条中文规则都不用，「第N章」零命中，整本书按「每 100 段一章」兜底成 1/2/3… 的序号目录，且这个 `en` 还会写进转换产物的 `dc:language`，把侧栏「从正文生成目录」的扫描规则一并带偏（同一份转换产物：en 规则命中 0、zh 规则命中 18）。现 TXT 导入的章节规则语言改为按书写系统判定（新增 `detectTxtLanguage`：假名→ja、谚文→ko，其余一律 zh——本项目不导入英文书，且 zh 规则集本身含 `chapter N` 写法），下载器头部的 ASCII 噪声完全不参与；小文件以全文取样，大文件以「头部 + 正文中段样本」取样。同一样本实测：语言 en→zh，章节从 34 个序号兜底变为 24 个真实章节（`usedFallback` true→false），虚拟目录扫描命中 0→24。
- **顶层拖拽导入 TXT 落进分组而非书库顶层（#3）**。去重命中返回的书会原样携带旧记录的 `groupId`/`groupName`，而顶层导入走的是"不动既有分组"的三态——该书曾以任何途径进过分组后，之后每次顶层拖入都会复活那条带分组的旧记录（EPUB 样本从未分组过，格式不对称只是表象）。现顶层**手动**导入（拖放/文件选择器）中，去重命中的书若带着非作者匹配组的旧分组，降回顶层并盖双时钟；书恰好在其作者匹配组内则保留并沿用既有去向反馈。受监视文件夹的静默重扫行为不变（否则每次重扫都会把手动分组的书拽回顶层），指定分组导入与目录导入也不受影响。
- **TXT 重复导入速度：先查重后转换（#4）**。导入热路径上，TXT 在整条转换管线（章节正则、段落兜底切分、EPUB 打包，大文件动辄数秒）跑完之后才按转换产物的 hash 查重——同一 TXT 重复拖入每次都要重转一遍才知道"已存在"。现 TXT 分支先对原始 TXT 算 partialMD5（只读文件首尾少数块，代价可忽略）并记录为 `sourceHash`，重导命中即直接短路返回既有条目，转换、解析、封面、落盘全部跳过；`overwrite` 导入、soft-deleted 复活、书文件缺失等场景不短路，走完整路径。新增 `importBook` 的 `txtDedupSkip` 埋点便于核对命中率。
- **鼠标侧键导航五处修复**（后退 = 书库上退一层分组 / 阅读器切上一本书，前进反之）：① 阅读器连按侧键时切书锁只维持一个微任务，而上一次换书的 `initViewState` 是异步的，下一次切书会在其初始化进行中关闭其视图，产生僵尸 viewState（内存泄漏）、误报「Unable to open book」与渲染竞争——现锁覆盖到初始化真正 settle，连按严格串行；② 书库「前进」栈只在面包屑导航时清空，从分组 A 后退后点击进入分组 B 再按前进会跳回过期的 A——现任何新分支导航（点击分组、详情模态跳虚拟分组等）都清空前进栈，且弹出目标所属文件夹已被删除时整栈作废而不是跳到不存在的分组；③ 书籍正文 iframe 内的侧键转发缺「左键按住（拖拽/划选中）」与「可编辑元素」两道守卫，划选文字途中误触侧键会突然切书丢选区——现已与窗口级路径守卫对齐；④ 侧键转发的 `iframe-side-button` 消息不校验来源——消费端现按实际来源放行（生产者是绑定在 iframe 文档上的主窗口 realm 函数，其 `window.postMessage` 为窗口内消息、source 即主窗口；若未来改为跨窗口生产则须为书籍 iframe），其余来源一律拒绝；⑤ 切换动画方向偏差：虚拟分组后退不设置方向属性沿用残留值、嵌套文件夹退回上级被判成前进动画——现按分组路径深度（退出虚拟分组恒为 back）判定滑动方向。
- **书内搜索高频字主线程冻结（实测 ~17 秒）**。高频字（如「的」）在扁平大 DOM 书籍（如 Pixiv 导出的单节近 9 万字、7000+ 文本节点）里搜索时，界面冻结由两段构成：每条匹配生成 CFI 时逐条重建各级祖先的 CFI 子节点列表（~6.3s），以及把几千条 CFI 回放成页内高亮时每条从 DOM 根重新定位、且整场回放作为微任务风暴挤在一个宏任务里（~12.2s 单块 longtask）。现做两层修复：① `epubcfi.js` 新增批次作用域的子节点列表记忆化（`createIndexCache`），`fromRange`/`toRange` 可注入缓存索引，同一文档上成批复用计算结果；foliate 视图在搜索回放与翻页/resize 高亮重放期间激活该缓存，CFI 生成侧 `batchedCfi` 同样接入；② 搜索高亮回放每 ~30ms 让出一帧并跨帧重置缓存，几千条高亮不再挤死主线程。真实样本实测：单条 CFI 定位 5.85ms → 0.03ms，主线程最长冻结 12.2s → ≤63ms，回放总耗时 12.2s → ~0.45s，CFI 生成 6.3s → ~0.1s。缓存严格限定在单个同步批次内（跨帧即作废），分页器重构 DOM 不会吃到失效缓存。
- **EPUB 虚拟目录：nav.json 污染的结构性免疫**。虚拟目录条目是用户数据（存 `Books/{hash}/config.json`），绝不应进入缓存文件 nav.json；此前止血只靠「nav 计算前剥离」的调用顺序，且生产环境命中缓存时永不重写，历史污染无法自愈，一旦虚拟目录不再应用就会以幽灵重复条目暴露。现改为三处出入口结构性拒绝：`computeBookNav` 入口过滤（剥离顺序不再是正确性前提）、`isBookNavCacheCurrent` 对含 CFI-href/负 id 条目的缓存判失效（下次打开即重算并回写干净缓存）、`saveBookNav` 写盘前过滤。
- **EPUB 虚拟目录：书名条目不再永久高亮**。单 section 书里指向整个正文文件的书名条目（无锚点、可导航跨度占全书 ≥90%）没有章节粒度，此前只要在读书就会被 foliate 的目录进度恒命中而永久点亮；现按「可导航跨度占比」将其排除出「当前章节」高亮（条目仍在列表中显示，点击跳转、侧栏滚动定位与自动展开不受影响）。
- **切书偶发 "Failed to open book"**。某次切书后 bookDoc 被以对象展开浅拷贝存入数据 store，类实例原型上的方法丢失，复用该书数据时目录管线调用 `splitTOCHref` 直接抛错；现在打开路径校验 bookDoc 可用性并对残废对象自愈。
- **点击目录/标注/搜索结果跳转后章节标题被页眉遮住上半截**。滚动模式下元素级跳转把目标精确对齐到滚动区顶部，而顶部悬停浮现的页眉（44px，与页眉开关无关）会盖住标题。现由渲染层新增 `overlay-top-inset` 设置在跳转落点上预留页眉高度（默认 0，行为不变；竖排滚动模式滚动轴为水平方向，不传）。
- **终审修复轮**：①内容检测为竖排的书在首次打开的会话内跳转落点沿水平轴偏移 44px（margin 布局 effect 依赖数组漏了 `vertical`，竖排同步后不重跑）；②无 slab 书籍生成过虚拟目录后侧栏入口被关闭、无法重新生成或改写正则；③「按文件分章」合成的目录条目不带 location，当前章节书本图标永不点亮；④生成目录在书籍已移出书库等异常时会误报保存成功（弹窗预检书库索引与既有 config，失败时报错且不落盘空白 config）；⑤章节正则缓存键缺分隔符，两组不同正则理论上可互串缓存。

[Unreleased]: https://github.com/bfzds/readest-local/compare/cef-preview-20260906-2...readest-local
