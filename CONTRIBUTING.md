# 贡献说明

本仓库是 Readest 的个人本地离线分支，仅供仓库所有者个人使用，**不是上游的协作仓库**：

- 不接受对外 Pull Request，也不承接上游的 issue / PR 流程。
- 本分支已移除云同步、在线服务、AI、订阅与遥测等功能，与上游的发展方向不同，不做双向合并。
- 如需了解上游项目，请自行查阅其官方渠道；本仓库不维护指向上游贡献流程的链接。

## 想自己改代码

构建与测试以本仓库自带的脚本为准：

| 用途 | 命令 / 脚本 |
| --- | --- |
| 启动桌面端开发 | `dev-desktop.ps1` |
| 打包便携版 | `build-portable.ps1` 或 `打包便携版.bat` |
| 前端单元测试 | 在 `apps/readest-app` 下运行 `pnpm test:pr:web:unit` |
| 类型检查与 lint | 在 `apps/readest-app` 下运行 `pnpm lint` |

开发环境需要 Node.js、pnpm 与 Rust（Tauri 桌面端）。

## 本地约定

- `AGENTS.md`：推送范围与脱敏要求（提交信息、代码、文档都适用）、与上游的关系（已彻底断开，见第 6 节）、语言支持范围（只维护中英）。
- `CLAUDE.md`：构建、清理与便携版数据的约定。
- `docs/git-setup.md`：Git 钩子与行尾设置。

改动入库前请先跑一遍单测和 lint，并确认符合上述约定。

## 许可

本项目基于 AGPL-3.0 协议发布，衍生修改须保留来源声明并继续以相同协议开源。
